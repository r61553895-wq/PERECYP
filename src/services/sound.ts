/**
 * Sound synthesis engine disabled completely per user request.
 * All functions are no-op to ensure zero audio playback.
 */

export function playClick(_enabled = true) {
  // Sounds completely disabled
}

export function playBuy(_enabled = true) {
  // Sounds completely disabled
}

export function playSell(_enabled = true) {
  // Sounds completely disabled
}

export function playProfit(_enabled = true) {
  // Sounds completely disabled
}

export function playNotification(_enabled = true) {
  // Sounds completely disabled
}

export function playError(_enabled = true) {
  // Sounds completely disabled
}

